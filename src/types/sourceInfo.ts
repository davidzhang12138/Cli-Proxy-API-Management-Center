export type SourceInfo = {
  displayName: string;
  type: string;
  identityKey?: string;
  rawName?: string;
};

export type CredentialInfo = {
  name: string;
  type: string;
  rawName?: string;
};
